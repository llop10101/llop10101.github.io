// Optional: Add interactivity to the grid (e.g., opening images in a lightbox)
const gridItems = document.querySelectorAll('.grid-item');

gridItems.forEach((item) => {
  item.addEventListener('click', () => {
    // Handle the click event, e.g., open the image in a lightbox
  });
});
